name: lint

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: fetch ruby version
        id: ruby-version
        run: |
          version=$(cat .ruby-version)
          echo "version=${version}" >> $GITHUB_OUTPUT

      - uses: ruby/setup-ruby@d2b39ad0b52eca07d23f3aa14fdf2a3fcc1f411c # pin@v1.148.0
        with:
          ruby-version: ${{ steps.ruby-version.outputs.version }}
          bundler-cache: true

      - name: lint
        run: bundle exec rubocop -c .rubocop.yml src/ spec/
